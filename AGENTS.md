# AGENTS.md

## Workspace status
- This workspace is currently empty or contains no committed project files.
- Treat this as an early-stage repository until project files appear.
- Do not make assumptions about build tools, language, framework, or test commands.

## Operating guidance
- Inspect the workspace before making changes.
- If a project is missing, ask the user for the repository purpose or target stack before proposing implementation work.
- When code appears, prefer the existing conventions and document them in this file only when they are not already discoverable elsewhere.
- Keep instructions concise and actionable.

## Default behavior
- Use the smallest change that satisfies the user request.
- Link to existing documentation when it exists; avoid duplicating it here.
- If there is no code to work on, focus on clarifying requirements rather than implementing.
